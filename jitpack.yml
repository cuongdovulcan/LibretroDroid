jdk:
  - openjdk11

before_install:
  - echo "Starting JitPack build for LibretroDroid"
  - chmod +x ./gradlew
  
install:
  - ./gradlew :libretrodroid:clean :libretrodroid:assembleRelease :libretrodroid:publishReleasePublicationToMavenLocal -x test --no-daemon --stacktrace
